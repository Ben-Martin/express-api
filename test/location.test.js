var expect    = require("chai").expect;

describe('Location model tests', function() {
    describe('Test to validate this is working', function() {
        it('does what we expect', function() {
            var hello = 'hello';

            expect(hello).to.equal('hello');
        });
    });
});